# heartbrain

A heart-brain dynamical system prototype, thought as the basic structure of emotional and conscious behaviors. 

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[experiments,dev]"
```
