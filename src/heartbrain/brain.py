"""Brain module.

The "brain" of this framework is a "Vanilla RNN".

In the early stages of the ``brain module`` implementation
we will use the ``functional programming`` paradigm;
Then we gradually move to the ``OOP`` paradigm when we need
states, methods and so on. 
"""